"use client";
import { useEffect, useRef, useState } from "react";
import { Reorder } from "framer-motion";
import type { Template } from "@/types";
import { createClient } from "@/lib/supabase/client";
const palettes=["#a78bfa","#f59e0b","#22d3ee","#d946ef","#34d399","#ef4444","#0ea5e9","#f97316"];
export default function CustomizeClient({initialTemplate,isAdmin}:{initialTemplate:Template;isAdmin:boolean}){const [template,setTemplate]=useState(initialTemplate); const [device,setDevice]=useState<"desktop"|"mobile">("desktop"); const [tab,setTab]=useState("sections"); const iframe=useRef<HTMLIFrameElement>(null); useEffect(()=>{iframe.current?.contentWindow?.postMessage({type:"VIBECODE_UPDATE",template},"*")},[template]); const updateHero=(key:string,value:string)=>setTemplate(t=>({...t,sections:t.sections.map(s=>s.type==="hero"?{...s,content:{...s.content,[key]:value}}:s)})); async function order(){const supabase=createClient(); const {data}=await supabase.auth.getSession(); const token=data.session?.access_token; const res=await fetch("/api/lead",{method:"POST",headers:{"Content-Type":"application/json",...(token?{Authorization:`Bearer ${token}`}:{})},body:JSON.stringify({templateId:template.id,templateName:template.name,selectedOptions:template,totalPrice:template.priceFrom,primaryColor:template.theme.primary,bgColor:template.theme.bgBase})}); if(res.ok && token) setTimeout(()=>location.href="/dashboard",1800)} function save(){const name=template.id.replace(/-([a-z])/g,(_,x)=>x.toUpperCase()); const blob=new Blob([`export const ${name} = ${JSON.stringify(template,null,2)} as const;`],{type:"text/typescript"}); const a=document.createElement("a"); a.href=URL.createObjectURL(blob); a.download=`${template.id}.ts`; a.click();}
return <main className="min-h-screen bg-slate-950 text-white"><div className="border-b border-white/10 p-3 flex justify-between"><b>Конструктор: {template.name}</b><div><button onClick={()=>setDevice("desktop")}>Desktop</button> <button onClick={()=>setDevice("mobile")}>Mobile</button></div></div><div className="md:hidden flex"><button className="flex-1 p-3" onClick={()=>setTab("sections")}>⚙️ Настройки</button><button className="flex-1 p-3" onClick={()=>setTab("preview")}>👁 Превью</button></div><div className="grid md:grid-cols-[300px_1fr] min-h-[calc(100vh-56px)]"><aside className={`${tab==="preview"?"hidden md:block":""} border-r border-white/10 p-4 space-y-4`}><div className="flex flex-wrap gap-2 text-sm">{["sections","order","colors","background",...(isAdmin?["style"]:[])].map(x=><button key={x} onClick={()=>setTab(x)} className="rounded-full bg-white/10 px-3 py-2">{x}</button>)}</div>{tab==="sections"&&<div className="space-y-3"><input className="w-full rounded-xl p-3 text-black" value={String(template.sections[0].content.headline)} onChange={e=>updateHero("headline",e.target.value)}/><textarea className="w-full rounded-xl p-3 text-black" value={String(template.sections[0].content.subheadline)} onChange={e=>updateHero("subheadline",e.target.value)}/>{isAdmin&&<input className="w-full rounded-xl p-3 text-black" placeholder="ADM telegramUsername"/>}</div>}{tab==="order"&&<Reorder.Group axis="y" values={template.sections} onReorder={sections=>setTemplate(t=>({...t,sections}))}>{template.sections.map(s=><Reorder.Item key={s.id} value={s} className="mb-2 rounded-xl bg-white/10 p-3 cursor-grab">{s.type}</Reorder.Item>)}</Reorder.Group>}{tab==="colors"&&<div className="grid grid-cols-4 gap-2">{palettes.map(p=><button key={p} style={{background:p}} className="h-10 rounded-xl" onClick={()=>setTemplate(t=>({...t,theme:{...t.theme,primary:p,gradientFrom:p}}))}/>)}</div>}{tab==="background"&&["bgBase","bgSurface","bgBorder"].map(k=><input key={k} type="color" value={(template.theme as any)[k].slice(0,7)} onChange={e=>setTemplate(t=>({...t,theme:{...t.theme,[k]:e.target.value}}))}/>) }{tab==="style"&&<select className="text-black" onChange={e=>setTemplate(t=>({...t,style:{...t.style,galleryStyle:e.target.value as any}}))}><option>grid</option><option>masonry</option><option>film</option></select>}<button id="lead" onClick={order} className="w-full rounded-full bg-cyan-300 px-5 py-3 font-bold text-black">Заказать</button>{isAdmin&&<button onClick={save}>💾 Сохранить шаблон клиента</button>}</aside><section className={`${tab!=="preview"?"hidden md:flex":"flex"} items-start justify-center bg-black/40 p-4`}><div className={device==="mobile"?"rounded-[2.5rem] border-8 border-zinc-800 p-2":"w-full"}><iframe ref={iframe} src={`/preview/${template.id}`} className="h-[80vh] bg-white" style={{width:device==="mobile"?393:"100%"}} /></div></section></div></main>}
