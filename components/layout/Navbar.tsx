"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { supabase } from "@/lib/supabase/client";
export function Navbar(){const [isAdmin,setIsAdmin]=useState(false); const [logged,setLogged]=useState(false); useEffect(()=>{if(!supabase)return; supabase.auth.getUser().then(async({data})=>{if(data.user){setLogged(true); const {data:profile}=await supabase!.from("profiles").select("role").eq("id",data.user.id).single(); setIsAdmin(profile?.role==="admin");}})},[]); return <nav className="sticky top-0 z-50 border-b border-white/10 bg-[#030712]/80 backdrop-blur"><div className="mx-auto flex max-w-6xl items-center justify-between px-4 py-3"><Link href="/" className="font-black">VIBECODE STUDIO</Link><div className="flex items-center gap-3 text-sm"><Link href="/templates">Шаблоны</Link>{isAdmin&&<Link href="/studio">⚡ Studio</Link>}{logged?<Link href={isAdmin?"/admin":"/dashboard"}>{isAdmin?"Админ-панель":"Личный кабинет"}</Link>:<Link href="/auth/login">Войти</Link>}</div></div></nav>}
